version: 2.1

orbs:
  cypress: cypress-io/cypress@1.24.0

workflows:
  build:
    jobs:
      - cypress/run:
          post-steps:
            - store_test_results:
                path: cypress/results/junit
            - store_artifacts:
                path: cypress/results/mochawesome/frontend-e2e-report.html